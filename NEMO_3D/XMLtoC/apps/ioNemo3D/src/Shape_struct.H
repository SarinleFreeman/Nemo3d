/* This file is automatically generated.  DO NOT EDIT! */

#ifndef _Shape_struct_H
#define _Shape_struct_H

#include "domStuff.H"
#include <vector>
#include <string>

/* Shape descriptor*/
class Shape_struct {
public:

    enum ShapeType {Box, Rhombohedron, Dome, Pyramid, Pyramid_spds, Pyramid_clipped, Cylinder, Cylinder_x, Ellipsoid, TiltedBox} Shape;       /* type of geometric object */
    short has_cell_granularity;       /* does this shape have cell granularity? */
    short use_for_strain_and_electronic;       /* also use this shape for
                                    electronic calculation */
    double xorigin;       /* minimum value in x-direction */
    double yorigin;       /* minimum value in y-direction */
    double zorigin;       /* minimum value in z-direction */
    double x;       /* maximum length in x-direction */
    double y;       /* maximum length in y-direction */
    double z;       /* maximum height in z-direction */
    short is_subdomain;       /* mark this for future use by local bandstructure calculation */
    short modify_species;       /* set species within this shape according to Mat, Mol, and Pol */
    vector<string> mat;       /* list of atom species */
    vector<double> mol;       /* concentrations of atom species */
    vector<int> pol;       /* polarity (use {0 0} for SiGe) */
    enum AlloyTreatmentType {VCA, RandomInUnitCell, RandomUnitCells, RandomCluster} AlloyTreatment;       /* algorithm for distributing atoms */
    short AlloyFixConcentration;       /* specify alloy concentration
                                    with absolute certainty */
    int AlloyClusterRadius;       /* radius of clustering in alloys */
    int matid3d;       /* You should not be seeing this. */

    Shape_struct(const DOM_Node&);
    Shape_struct()  {;}
    ~Shape_struct() {;}

    void set(const DOM_Node&, string);
    void setFromArrayElement(const DOM_Node&,  string, int);
    string toString(int);

    ShapeType getShapeType(const string& s) {
        if (s=="Box")  return Box;
        if (s=="Rhombohedron")  return Rhombohedron;
        if (s=="Dome")  return Dome;
        if (s=="Pyramid")  return Pyramid;
        if (s=="Pyramid_spds")  return Pyramid_spds;
        if (s=="Pyramid_clipped")  return Pyramid_clipped;
        if (s=="Cylinder")  return Cylinder;
        if (s=="Cylinder_x")  return Cylinder_x;
        if (s=="Ellipsoid")  return Ellipsoid;
        if (s=="TiltedBox")  return TiltedBox;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return Box;
    }

    string getEnumStringShapeType(const ShapeType& e) {
        if (e==Box)  return string("Box");
        if (e==Rhombohedron)  return string("Rhombohedron");
        if (e==Dome)  return string("Dome");
        if (e==Pyramid)  return string("Pyramid");
        if (e==Pyramid_spds)  return string("Pyramid_spds");
        if (e==Pyramid_clipped)  return string("Pyramid_clipped");
        if (e==Cylinder)  return string("Cylinder");
        if (e==Cylinder_x)  return string("Cylinder_x");
        if (e==Ellipsoid)  return string("Ellipsoid");
        if (e==TiltedBox)  return string("TiltedBox");
        return string("");
    }

    AlloyTreatmentType getAlloyTreatmentType(const string& s) {
        if (s=="VCA")  return VCA;
        if (s=="RandomInUnitCell")  return RandomInUnitCell;
        if (s=="RandomUnitCells")  return RandomUnitCells;
        if (s=="RandomCluster")  return RandomCluster;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return VCA;
    }

    string getEnumStringAlloyTreatmentType(const AlloyTreatmentType& e) {
        if (e==VCA)  return string("VCA");
        if (e==RandomInUnitCell)  return string("RandomInUnitCell");
        if (e==RandomUnitCells)  return string("RandomUnitCells");
        if (e==RandomCluster)  return string("RandomCluster");
        return string("");
    }

};


#endif
