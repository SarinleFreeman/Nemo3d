/* This file is automatically generated.  DO NOT EDIT! */

#ifndef _Phys_struct_H
#define _Phys_struct_H

#include "domStuff.H"
#include <vector>
#include <string>

/* physical parameters including potentials*/
class Phys_struct {
public:

    string PotentialFile;       /* File describing electrostatic potential */
    short MagneticFieldOn;       /* is magnetic field on? */
    double Bx;       /* x component of magnetic field (Tesla) */
    double By;       /* y component of magnetic field (Tesla) */
    double Bz;       /* z component of magnetic field (Tesla) */
    string Bgauge;       /* Gauge for the magnetic field, 'Symmetric' gives A = (-By/2,Bx/2,0), 'Asymmetric_x' gives  A = (0,Bx,0) useful with Efield along x, 'Asymmetric_y' gives  A = (-By,0,0), useful with Efield along y */
    int ImpNumber;       /* Number of P+ donors in the system (max.3) */
    vector<double> ImpsXYZ;       /* ImpsXYZ */
    int imp_model;       /* Use 1 to include U0 only, 2 includes also Si:P parameters */
    short include_nuclear_term;       /* include_nuclear_term */
    short EfieldON;       /* EfieldON */
    double Ex;       /* x component of electric field (eV) */
    double Ey;       /* y component of electric field (eV) */
    double Ez;       /* z component of electric field (eV) */
    short donor_file;       /* donor_file */
    short electric_file;       /* electric_file */
    double U0;       /* On site potential U0 (eV) */
    enum SurfacePassivationType {PassivateDanglingBond, PassivateOrbital} SurfacePassivation;       /* method by which oribitals of surface atoms
                            couple to vacuum */
    double DanglingBondEnergyShift;       /* energy shift for dangling bonds on the
                            surface (eV) */
    double BondShift_s;       /* s-orbital energy shift of surface atom */
    double BondShift_p;       /* p orbital energy shift of surface atom */
    double BondShift_d;       /* d orbital energy shift of surface atom */
    double BondShift_sstar;       /* s* orbital energy shift of surface atom */
    double Disorder1;       /* NONE */
    double HbarOverTau;       /* NONE */

    Phys_struct(const DOM_Node&);
    Phys_struct()  {;}
    ~Phys_struct() {;}

    void set(const DOM_Node&, string);
    void setFromArrayElement(const DOM_Node&,  string, int);
    string toString(int);

    SurfacePassivationType getSurfacePassivationType(const string& s) {
        if (s=="PassivateDanglingBond")  return PassivateDanglingBond;
        if (s=="PassivateOrbital")  return PassivateOrbital;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return PassivateDanglingBond;
    }

    string getEnumStringSurfacePassivationType(const SurfacePassivationType& e) {
        if (e==PassivateDanglingBond)  return string("PassivateDanglingBond");
        if (e==PassivateOrbital)  return string("PassivateOrbital");
        return string("");
    }

};


#endif
