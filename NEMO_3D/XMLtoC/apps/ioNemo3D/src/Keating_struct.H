/* This file is automatically generated.  DO NOT EDIT! */

#ifndef _Keating_struct_H
#define _Keating_struct_H

#include "domStuff.H"
#include <vector>
#include <string>

/* keating parameters*/
class Keating_struct {
public:

    enum BoundCondType {AllFree, FixOne, FixSurfaceAll, FixInterior, FixZmin} BoundCond;       /* impose constraint on configuration */
    enum Periodicity_strainType {SameAsGlobal, Closed, Periodic_xyz, Periodic_x, Periodic_y, Periodic_z, Periodic_xy, Periodic_yz, Periodic_xz} Periodicity_strain;       /* periodicity for strain calculation */
    enum MinWrtLattType {none, x, y, z, xy, xz, yz, xyz} MinWrtLatt;       /* allow the periodicity to relax */
    short UpdateLatt;       /* update lattice constants before strain calculation? */
    vector<double> UpdateLattConst;       /* If UpdateLatt is true, what are the new lattice constants? */
    double SCALE_FCTR;       /* What is the scaling factor (S) for dE/da?
                                  Use number between 1e-2 to 1e-5 depending 
                                  on the number of atoms (N) in strain calculation.
                                  An ideal S is about 1.0/sqrt(N). 
                             */
    int MaxIter;       /* maximum number of interations */
    double tol;       /* tolerance */
    enum ConvCritType {Gradient, Step} ConvCrit;       /* NONE */
    int strainCheckPointInterval;       /* Strain iteration checkpointing interval */
    string StartFileRead;       /* NONE */
    int verbose;       /* NONE */

    Keating_struct(const DOM_Node&);
    Keating_struct()  {;}
    ~Keating_struct() {;}

    void set(const DOM_Node&, string);
    void setFromArrayElement(const DOM_Node&,  string, int);
    string toString(int);

    BoundCondType getBoundCondType(const string& s) {
        if (s=="AllFree")  return AllFree;
        if (s=="FixOne")  return FixOne;
        if (s=="FixSurfaceAll")  return FixSurfaceAll;
        if (s=="FixInterior")  return FixInterior;
        if (s=="FixZmin")  return FixZmin;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return AllFree;
    }

    string getEnumStringBoundCondType(const BoundCondType& e) {
        if (e==AllFree)  return string("AllFree");
        if (e==FixOne)  return string("FixOne");
        if (e==FixSurfaceAll)  return string("FixSurfaceAll");
        if (e==FixInterior)  return string("FixInterior");
        if (e==FixZmin)  return string("FixZmin");
        return string("");
    }

    Periodicity_strainType getPeriodicity_strainType(const string& s) {
        if (s=="SameAsGlobal")  return SameAsGlobal;
        if (s=="Closed")  return Closed;
        if (s=="Periodic_xyz")  return Periodic_xyz;
        if (s=="Periodic_x")  return Periodic_x;
        if (s=="Periodic_y")  return Periodic_y;
        if (s=="Periodic_z")  return Periodic_z;
        if (s=="Periodic_xy")  return Periodic_xy;
        if (s=="Periodic_yz")  return Periodic_yz;
        if (s=="Periodic_xz")  return Periodic_xz;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return SameAsGlobal;
    }

    string getEnumStringPeriodicity_strainType(const Periodicity_strainType& e) {
        if (e==SameAsGlobal)  return string("SameAsGlobal");
        if (e==Closed)  return string("Closed");
        if (e==Periodic_xyz)  return string("Periodic_xyz");
        if (e==Periodic_x)  return string("Periodic_x");
        if (e==Periodic_y)  return string("Periodic_y");
        if (e==Periodic_z)  return string("Periodic_z");
        if (e==Periodic_xy)  return string("Periodic_xy");
        if (e==Periodic_yz)  return string("Periodic_yz");
        if (e==Periodic_xz)  return string("Periodic_xz");
        return string("");
    }

    MinWrtLattType getMinWrtLattType(const string& s) {
        if (s=="none")  return none;
        if (s=="x")  return x;
        if (s=="y")  return y;
        if (s=="z")  return z;
        if (s=="xy")  return xy;
        if (s=="xz")  return xz;
        if (s=="yz")  return yz;
        if (s=="xyz")  return xyz;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return none;
    }

    string getEnumStringMinWrtLattType(const MinWrtLattType& e) {
        if (e==none)  return string("none");
        if (e==x)  return string("x");
        if (e==y)  return string("y");
        if (e==z)  return string("z");
        if (e==xy)  return string("xy");
        if (e==xz)  return string("xz");
        if (e==yz)  return string("yz");
        if (e==xyz)  return string("xyz");
        return string("");
    }

    ConvCritType getConvCritType(const string& s) {
        if (s=="Gradient")  return Gradient;
        if (s=="Step")  return Step;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return Gradient;
    }

    string getEnumStringConvCritType(const ConvCritType& e) {
        if (e==Gradient)  return string("Gradient");
        if (e==Step)  return string("Step");
        return string("");
    }

};


#endif
