/* This file is automatically generated.  DO NOT EDIT! */

#ifndef _Dev_struct_H
#define _Dev_struct_H

#include "domStuff.H"
#include <vector>
#include <string>
#include "Shape_struct.H"

/* Geometric specification of simulation*/
class Dev_struct {
public:

    string StructFile;       /* only NULL (i.e. do NOT read structure info of
                        first shape from file) is implemented) */
    enum CrystalStructType {Cubic, Diamond, Zincblende, Diamond_FCC, Zincblende_FCC, Wurtzite, ZB_110, ZB_110_small, ZB_111, CellUserDef} CrystalStruct;       /* crystal structure */
    enum band_modelType {Bands_1_s_nospin, Bands_10_sp3ss_spin, Bands_10_sp3d5ss_nospin, Bands_20_sp3d5ss_spin} band_model;       /* basis for electronic calculation */
    double a_lattice_x;       /* lattice constant in x-dir */
    double a_lattice_y;       /* lattice constant in y-dir */
    double a_lattice_z;       /* lattice constant in z-dir */
    short tilt_hkl;       /* hkl defined below, describe the direction of the tilt: hkl=1 0 0 means no tilt and 28 0 1 means a ~2-DEG tilt in plane XZ with respect to a_z */
    int tilt_h;       /* tilt_h */
    int tilt_k;       /* tilt_k */
    int tilt_l;       /* tilt_l */
    int NaddALY;       /* # of additional layers to include in y-direction: use this option if you want to describe an structure with a fraction of the unit cell, which would be measured in units of eg. a_y/4 for Zincblende */
    int NaddALZ;       /* # of additional layers to include in z-direction */
    vector<Shape_struct> ShapeList;       /* array of shapes */

    Dev_struct(const DOM_Node&);
    Dev_struct()  {;}
    ~Dev_struct() {;}

    void set(const DOM_Node&, string);
    void setFromArrayElement(const DOM_Node&,  string, int);
    string toString(int);

    CrystalStructType getCrystalStructType(const string& s) {
        if (s=="Cubic")  return Cubic;
        if (s=="Diamond")  return Diamond;
        if (s=="Zincblende")  return Zincblende;
        if (s=="Diamond_FCC")  return Diamond_FCC;
        if (s=="Zincblende_FCC")  return Zincblende_FCC;
        if (s=="Wurtzite")  return Wurtzite;
        if (s=="ZB_110")  return ZB_110;
        if (s=="ZB_110_small")  return ZB_110_small;
        if (s=="ZB_111")  return ZB_111;
        if (s=="CellUserDef")  return CellUserDef;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return Cubic;
    }

    string getEnumStringCrystalStructType(const CrystalStructType& e) {
        if (e==Cubic)  return string("Cubic");
        if (e==Diamond)  return string("Diamond");
        if (e==Zincblende)  return string("Zincblende");
        if (e==Diamond_FCC)  return string("Diamond_FCC");
        if (e==Zincblende_FCC)  return string("Zincblende_FCC");
        if (e==Wurtzite)  return string("Wurtzite");
        if (e==ZB_110)  return string("ZB_110");
        if (e==ZB_110_small)  return string("ZB_110_small");
        if (e==ZB_111)  return string("ZB_111");
        if (e==CellUserDef)  return string("CellUserDef");
        return string("");
    }

    band_modelType getband_modelType(const string& s) {
        if (s=="Bands_1_s_nospin")  return Bands_1_s_nospin;
        if (s=="Bands_10_sp3ss_spin")  return Bands_10_sp3ss_spin;
        if (s=="Bands_10_sp3d5ss_nospin")  return Bands_10_sp3d5ss_nospin;
        if (s=="Bands_20_sp3d5ss_spin")  return Bands_20_sp3d5ss_spin;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return Bands_1_s_nospin;
    }

    string getEnumStringband_modelType(const band_modelType& e) {
        if (e==Bands_1_s_nospin)  return string("Bands_1_s_nospin");
        if (e==Bands_10_sp3ss_spin)  return string("Bands_10_sp3ss_spin");
        if (e==Bands_10_sp3d5ss_nospin)  return string("Bands_10_sp3d5ss_nospin");
        if (e==Bands_20_sp3d5ss_spin)  return string("Bands_20_sp3d5ss_spin");
        return string("");
    }

};


#endif
