/* This file is automatically generated.  DO NOT EDIT! */

#ifndef _Absorp_struct_H
#define _Absorp_struct_H

#include "domStuff.H"
#include <vector>
#include <string>

/* absorption parameters*/
class Absorp_struct {
public:

    short AbsCoeff;       /* AbsCoeff */
    short IntegrAbs;       /* IntegrAbs */
    double HbarOverTau;       /* HbarOverTau */
    enum rampType {fermi, temp, strength, phi, theta} ramp;       /* ramp */

    Absorp_struct(const DOM_Node&);
    Absorp_struct()  {;}
    ~Absorp_struct() {;}

    void set(const DOM_Node&, string);
    void setFromArrayElement(const DOM_Node&,  string, int);
    string toString(int);

    rampType getrampType(const string& s) {
        if (s=="fermi")  return fermi;
        if (s=="temp")  return temp;
        if (s=="strength")  return strength;
        if (s=="phi")  return phi;
        if (s=="theta")  return theta;
        cerr << "Invalid string!  ( returning arbitrary value! )" << endl;
        return fermi;
    }

    string getEnumStringrampType(const rampType& e) {
        if (e==fermi)  return string("fermi");
        if (e==temp)  return string("temp");
        if (e==strength)  return string("strength");
        if (e==phi)  return string("phi");
        if (e==theta)  return string("theta");
        return string("");
    }

};


#endif
